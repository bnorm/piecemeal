pluginManagement {
  plugins {
  }
  repositories {
    mavenCentral()
    mavenLocal()
  }
}

dependencyResolutionManagement {
  repositories {
    mavenCentral()
    mavenLocal()
  }
}

rootProject.name = "piecemeal-sample"

//includeBuild("..")
